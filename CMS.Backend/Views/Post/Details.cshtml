@model CMS.Data.Entities.Post

@{
    bool isAdmin = User.Identity != null && User.Identity.IsAuthenticated;
    Layout = isAdmin ? "_LayoutAdmin" : "_Layout";
    ViewData["Title"] = Model.Title;
}

@if (isAdmin)
{
    <div class="card-premium">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1 class="h3 mb-0 fw-bold"><i class="bi bi-file-earmark-post-fill text-muted"></i> Chi tiết bài viết (Quản trị)</h1>
            <div class="d-flex gap-2">
                <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning py-1.5"><i class="bi bi-pencil-square"></i> Sửa bài viết</a>
                <a asp-action="Index" class="btn btn-premium-outline py-1.5 text-decoration-none">Quay lại danh sách</a>
            </div>
        </div>

        <div class="row">
            <div class="col-md-8">
                <span class="badge bg-light text-dark rounded-pill px-3 py-1.5 mb-3">
                    @(Model.Category?.Name ?? "N/A")
                </span>
                <h2 class="fw-bold text-black mb-3">@Model.Title</h2>
                <p class="text-muted small mb-4"><i class="bi bi-calendar3"></i> Ngày đăng: @Model.CreatedDate.ToString("dd/MM/yyyy")</p>
                <hr class="my-4" />
                <div class="post-content lh-lg text-secondary">
                    @Html.Raw(Model.Content)
                </div>
            </div>
            <div class="col-md-4">
                @if (!string.IsNullOrEmpty(Model.ImageUrl))
                {
                    <img src="@Model.ImageUrl" alt="@Model.Title" class="img-fluid rounded-3 border" style="max-height: 300px; width: 100%; object-fit: cover;" />
                }
            </div>
        </div>
    </div>
}
else
{
    <!-- Guest Public View -->
    <div class="container my-5" style="max-width: 800px;">
        <div class="text-center mb-5">
            <a href="/" class="text-decoration-none text-muted small"><i class="bi bi-arrow-left"></i> Quay lại trang chủ</a>
            <h1 class="display-5 fw-bold text-black mt-3 mb-3">@Model.Title</h1>
            <div class="text-muted small">
                <span class="badge bg-light text-dark rounded-pill px-3 py-1.5 me-2">@(Model.Category?.Name ?? "N/A")</span>
                <span><i class="bi bi-calendar3"></i> Đăng ngày: @Model.CreatedDate.ToString("dd/MM/yyyy")</span>
            </div>
        </div>

        @if (!string.IsNullOrEmpty(Model.ImageUrl))
        {
            <div class="text-center mb-5">
                <img src="@Model.ImageUrl" alt="@Model.Title" class="img-fluid rounded-4 shadow-sm" style="max-height: 400px; width: 100%; object-fit: cover;" />
            </div>
        }

        <article class="post-content lh-lg text-secondary" style="font-family: 'Inter', sans-serif; font-size: 1.1rem;">
            @Html.Raw(Model.Content)
        </article>
    </div>
}
